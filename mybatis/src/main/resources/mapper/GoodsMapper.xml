<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ksmart.mybatis.mapper.GoodsMapper">
	<resultMap type="Goods" id="goodsResultMap">
			<id 	column="g_code" 		property="goodsCode"/>
			<result column="g_name" 		property="goodsName"/>
			<result column="g_price" 		property="goodsPrice"/>
			<result column="g_seller_id" 	property="goodsSellerId"/>
			<result column="g_reg_date" 	property="goodsRegDate"/>	

	</resultMap>
	
	<!-- 판매자 자신의 상품목록 조회 -->
	<select id="getResisInfoList" fetchSize="100" resultMap="goodsResultMap">
		SELECT 
			 m.m_id
			,g.g_code
			,g.g_name
			,g.g_price
			,g.g_seller_id
			,g.g_reg_date		
		FROM 
			tb_member AS m
			INNER JOIN 
			tb_goods AS g
			ON 
			m.m_id = g.g_seller_id
		ORDER BY m.m_id, g.g_code;			
	</select>
	
	<!-- 상품등록 -->
	<insert id="addGoods" parameterType="Goods">
		INSERT INTO tb_goods
		(		
			   g_code
			  ,g_name
			  ,g_price
			  ,g_seller_id
			  ,g_reg_date	
		)VALUES(
			   #{goodsCode}
			  ,#{goodsName}
			  ,#{goodsPrice}
			  ,#{goodsSellerId}
			  ,CURDATE()
		);
	</insert>
</mapper>  
  